import { Props, Handler } from './types';
declare const _default: ({ columns, filename, params, requestUrl, submitUrl, formatRequestParams, formatSubmitParams }: Props) => {
    onImport: Handler;
    onExport: Handler;
};
export default _default;
